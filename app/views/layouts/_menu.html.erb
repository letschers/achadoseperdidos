

<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: "navbar-brand" do |l| %>
      Achados e Perdidos
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse pull-right" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= menuActive(new_dado_path) %>">

          <% if @current_user.nil? %>

          <% else %>

          <%= link_to t(:new), new_dado_path %> <span class="sr-only">(current)</span>


          <% end %>
        </li>

        <li class="<%= menuActive(achados_path) %>">
          <%= link_to t(:achados),achados_path %>
        </li>

        <li class="<%= menuActive(perdidos_path) %>"> 
          <%= link_to t(:perdidos), perdidos_path %> 
        </li>

        <li class="<%= menuActive(arquivados_path) %>"> 
          <%= link_to t(:arquivados), arquivados_path %> 
        </li>


        <li class="<%= menuActive(login_path) %>"> 
          <% if @current_user.nil? %>
          <%= link_to t(:login), login_path %> 
        </li>
        <% else %>

        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
             <%= @current_user.nome %> <span class="caret"></span>
           </a>
           <ul class="dropdown-menu">
            <li class="">
              <%= link_to t(:logout), logout_path %> 
            </li>   
          </ul>
        </li>
      </ul>

      <% end %>
    </li>

  </ul>
</div>
</div>
</nav>